const body=document.body;const langWrap=document.querySelector('.lang-wrap');const langBtn=document.querySelector('.btn-lang');
if(langBtn){langBtn.addEventListener('click',()=>{langWrap.classList.toggle('open');langBtn.setAttribute('aria-expanded',langWrap.classList.contains('open'));});document.addEventListener('click',e=>{if(!langWrap.contains(e.target))langWrap.classList.remove('open');});}
const drawer=document.getElementById('mobile-drawer');const burger=document.querySelector('.burger');const overlay=document.querySelector('.drawer-overlay');const closeBtn=document.querySelector('.drawer-close');
let lastFocus=null;function trap(container){const focusables=container.querySelectorAll('a,button,[href],input');if(!focusables.length)return;const first=focusables[0],last=focusables[focusables.length-1];container.addEventListener('keydown',e=>{if(e.key==='Tab'){if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});}
function openDrawer(){lastFocus=document.activeElement;drawer.classList.add('open');overlay.classList.add('show');drawer.setAttribute('aria-hidden','false');burger.setAttribute('aria-expanded','true');body.style.overflow='hidden';trap(drawer);drawer.querySelector('a,button').focus();}
function closeDrawer(){drawer.classList.remove('open');overlay.classList.remove('show');drawer.setAttribute('aria-hidden','true');burger.setAttribute('aria-expanded','false');body.style.overflow='';if(lastFocus)lastFocus.focus();}
if(burger){burger.addEventListener('click',openDrawer);closeBtn.addEventListener('click',closeDrawer);overlay.addEventListener('click',closeDrawer);document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeDrawer();closePrivacy();}});}
const allDetails=[...document.querySelectorAll('.faq details')];allDetails.forEach(d=>d.addEventListener('toggle',()=>{if(d.open)allDetails.forEach(o=>o!==d&&(o.open=false));}));
const modal=document.querySelector('.privacy-modal');const openLink=document.querySelector('[data-open-privacy]');const closeLink=document.querySelector('[data-close-privacy]');const closeX=document.querySelector('.privacy-x');
function openPrivacy(e){e.preventDefault();modal.hidden=false;body.style.overflow='hidden';closeX.focus();}
function closePrivacy(){if(!modal)return;modal.hidden=true;body.style.overflow='';}
if(openLink){openLink.addEventListener('click',openPrivacy);closeLink.addEventListener('click',closePrivacy);closeX.addEventListener('click',closePrivacy);modal.addEventListener('click',e=>{if(e.target===modal)closePrivacy();});}
const io=new IntersectionObserver(entries=>entries.forEach(en=>{if(en.isIntersecting)en.target.classList.add('show');}),{threshold:.16});document.querySelectorAll('section article,.visual-card,.lead-form').forEach(el=>{el.classList.add('reveal');io.observe(el);});
